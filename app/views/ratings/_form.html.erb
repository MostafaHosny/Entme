<%= form_with(model: rating, local: true) do |form| %>
  <% if rating.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rating.errors.count, "error") %> prohibited this rating from being saved:</h2>

      <ul>
      <% rating.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :rating_user_id %>
  </div>

  <div class="field">
    <%= form.label :stars %>
    <%= form.number_field :stars, id: :rating_stars %>
  </div>

  <div class="field">
    <%= form.label :comment %>
    <%= form.text_field :comment, id: :rating_comment %>
  </div>

  <div class="field">
    <%= form.label :rateable_id %>
    <%= form.number_field :rateable_id, id: :rating_rateable_id %>
  </div>

  <div class="field">
    <%= form.label :rateable_type %>
    <%= form.text_field :rateable_type, id: :rating_rateable_type %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
